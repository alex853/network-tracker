version '1.0-SNAPSHOT'

apply plugin: 'java'

//archivesBaseName = 'network-view-core'

repositories {
    mavenCentral()
    mavenLocal()
    maven {
        url 'https://mymavenrepo.com/repo/9k6aQjU4o2RPUv5zfolv/' // simforge repo read url
    }
}

dependencies {
    compile group: 'net.simforge', name: 'simforge-commons', version: '1.0.4'

    testCompile group: 'junit', name: 'junit', version: '4.11'
}

//mainClassName = 'net.simforge.commons.legacy.runner.RunnerMT'
